
<% @post.comments.each do |post| %>
    <%= post.body %>
<% end %>

<h3>Comments</h3>

<%= form_with(model: [ @post, @comment]) do |form| %>
  <%= form.label :body %>
  <%= form.text_area :body %>
  <%= form.submit "Add Comment" %>
<% end%>

<p>
  <strong>Name:</strong>
  <%= @post.body %>
</p>
